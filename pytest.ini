[pytest]
testpaths = backend/test
python_files = test_*.py
addopts = -ra --cov=backend/app --cov-report=term-missing --cov-fail-under=65
markers =
    unit: Unit tests for services and helpers
    integration: Integration tests for API endpoints
    e2e: End-to-end business scenario tests
